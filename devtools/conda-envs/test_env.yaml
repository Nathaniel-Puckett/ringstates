name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Dependencies
  - numpy
  - matplotlib

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - networkx
    - photonic_circuit_solver
    - qiskit
    - qiskit_aer