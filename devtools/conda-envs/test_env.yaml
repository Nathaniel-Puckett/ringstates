name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Dependencies
  - networkx
  - numpy
  - matplotlib
  - photonic_circuit_solver
  - qiskit
  - qiskit_aer

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

